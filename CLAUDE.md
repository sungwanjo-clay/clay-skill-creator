@AGENTS.md

## Claude Code

The imported file is the whole brief. This file exists because Claude Code reads `CLAUDE.md` and
not `AGENTS.md`, and importing is how both hosts read one copy instead of two that drift.
